<div class="container">
  <div class="row mt-2 mb-2">
    <div class="col">
    <h4>検索結果</h4>
      全<%= @records.count %>件
      <% if @records.empty? %>
        <p style="color:red;">該当する投稿はありません</p></div>
      <% else %>
        <%= render 'public/posts/index', posts: @records %>
      <% end %>
      
    </div>
  </div>
</div>